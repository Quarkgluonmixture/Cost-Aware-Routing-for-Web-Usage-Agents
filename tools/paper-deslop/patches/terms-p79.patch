--- /tmp/claude-1012/-home-jiaming-workspace-Cost-Aware-Routing-for-Web-Usage-Agents/9c564995-f040-44eb-872f-283f8a45a923/scratchpad/paper-deslop/terms.txt	2026-07-24 22:25:00.585396760 +0100
+++ tools/paper-deslop/terms.txt	2026-07-24 22:34:11.509377193 +0100
@@ -11,13 +11,54 @@
 # would the paper lose a distinguishable, citable, operationalized concept?
 # One term per line; matching is case-insensitive and treats spaces/hyphens
 # as interchangeable. Lines starting with # are comments.
+#
+# WARNING - matching is SUBSTRING, not word-bounded: invariant_check.term_counts
+# builds `[\s~-]+`-joined regexes with no \b. Short abbreviations therefore
+# false-match. Measured on the P79 drafts: "DOM" hits random/dominance/dominant/
+# domain 90 times, "SoM" hits some/sometimes 7 times. Keep entries to multi-word
+# phrases or long unique tokens; never add a 2-4 letter abbreviation.
+#
+# P79-specific list (2026-07-24). Counts in comments = occurrences across
+# docs/checkpoints/paper_drafts/ at the time of writing.
 
+# --- Phantom routing space (paper-1 core construct) ---
+phantom routing space
+phantom arm
+phantom mode
+phantom prompt
+P-SoM
+P-text
+P-prompt
+drop-one oracle
+complementarity
+
+# --- Representation / observation vocabulary ---
 representation routing
+observation mode
+annotated image
+accessibility tree
+AXTree
+SOM_MARKS
+
+# --- Routing layer ---
+cost-aware routing
+learned router
+rule-based router
+routing signal
 cost-accuracy trade-off
-multimodal web agent
 selective classification
-out-of-distribution generalization
-nested cross-validation
-mutual-information feature selection
+
+# --- Benchmark, models, agents ---
+VisualWebArena
+multimodal web agent
+Qwen3-VL
+Gemma3-VL
+scaffold
+
+# --- Metrics and statistics ---
+adjusted_success
+AUROC
+Pareto
+fixed-effect
+inverse-variance
 premature termination
-phantom prompt
