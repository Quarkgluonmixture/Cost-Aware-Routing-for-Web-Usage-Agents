A High Ceiling You Cannot Reach: Label Supply Blocks Learned Representation Routing for Web Agents

<!--
Build input, not prose: convert.sh reads the first non-empty line above as the
LaTeX \title. Keep it to one line and do not add Markdown emphasis, which
pandoc would carry into the title box.

Rationale for this wording (2026-07-27): the two halves are the two results in
order. The ceiling is real and large (§3), and the reason it is unreachable is
the rate at which labels are produced (§4), not the hypothesis class and not
how the label is defined (§6). "Cannot reach" is the claim the evidence
supports; "cannot be learned in principle" is not, and §8.2 says so.
-->
