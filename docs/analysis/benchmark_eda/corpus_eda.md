# Benchmark corpus EDA — VWA + WA

Generated by `scripts/analysis/benchmark_eda.py` from the `.raw.json` task configs (placeholder URLs, host-independent). Thesis rubric #10.


## 0. From corpus to scored set

Every success rate in this thesis has one of the `scored` numbers below as its denominator. The two exclusion layers differ in *when* they apply, and the distinction is operational, not cosmetic:

- **N/A** — removed at task-**load** time; the runner never sees these episodes. All are `string_match` with `reference_answers.fuzzy_match == "N/A"`. Under our no-N/A-exit agent prompt they are un-passable, and the evaluator cannot separate a reasoned N/A judgement from an early exit (§139.8, preregistered).
- **protocol** — removed at **analysis** time only; the runner keeps collecting them deliberately, so the exclusion stays auditable and reversible (AMENDMENT_08–10).


Because the layers fire at different times, **two different counts are both correct** and the project uses both. Quote whichever one your denominator actually is:

| corpus | corpus size | − N/A | **run set** | − protocol | **scored set** |
|---|---:|---:|---:|---:|---:|
| VWA classifieds | 234 | 10 | **224** | 0 | **224** |
| VWA reddit | 210 | 5 | **205** | 2 (160, 58) | **203** |
| VWA shopping | 466 | 31 | **435** | 3 (463, 465, 345) | **432** |
| WA reddit | 106 | 2 | **104** | 0 | **104** |
| WA shopping | 192 | 19 | **173** | 0 | **173** |
| WA shopping_admin | 182 | 6 | **176** | 0 | **176** |

- **run set** = episodes a completed run actually contains. Verified against real runs: `B1_dom_classifieds` → 224 episodes, `B1_dom_reddit` → **205**, WA `B1_dom_wa_reddit` → **104**.
- **scored set** = denominator of every reported success rate: VWA reddit **203**, VWA shopping **432**.

This is the source of a long-standing double set of numbers in the project notes — reddit appears as both 205 and 203, shopping as both 435 and 432. Neither is wrong; they are the run set and the scored set. An episode count that matches the *scored* number would actually indicate a problem, because protocol-excluded episodes are supposed to be collected.


### 0.1 Corpus defects found in the shipped annotations

Difficulty labels outside the intended `{easy, medium, hard}` vocabulary. Reported rather than auto-corrected: a typo'd level folded silently into the nearest label means a stratified analysis runs on a different partition than the one it documents.

| corpus | task_id | field | value | likely intent |
|---|---:|---|---|---|
| VWA classifieds | 161 | `visual_difficulty` | `mediun` | `medium` |
| VWA shopping | 179 | `reasoning_difficulty` | `hrad` | `hard` |

Each affects a single task, so the effect on any aggregate is negligible; the reason to record them is that a difficulty-stratified table would otherwise silently grow an extra one-task stratum.


## 1. Corpus at a glance

| corpus | tasks | distinct templates | tasks/template (med / max) | login | reset | with ref image |
|---|---:|---:|---:|---:|---:|---:|
| VWA classifieds | 234 | 75 | 3 / 7 | 234 | 22 | 68 (29.1%) |
| VWA reddit | 210 | 87 | 2 / 14 | 81 | 21 | 84 (40.0%) |
| VWA shopping | 466 | 152 | 3.0 / 12 | 346 | 19 | 169 (36.3%) |
| WA reddit | 106 | 21 | 5 / 6 | 106 | 0 | 0 (0.0%) |
| WA shopping | 192 | 49 | 5 / 6 | 192 | 0 | 0 (0.0%) |
| WA shopping_admin | 182 | 41 | 5 / 7 | 182 | 0 | 0 (0.0%) |

**Task diversity is well below task count.** A template is one question shape with different slot fills, so the distinct-template column is the honest denominator for 'how many different things are we asking'.


## 2. Reference images — the axis that separates the two benchmarks

| corpus | tasks with ≥1 ref image | total ref images | multi-image tasks |
|---|---:|---:|---:|
| VWA classifieds | 68 (29.1%) | 68 | 0 |
| VWA reddit | 84 (40.0%) | 89 | 4 |
| VWA shopping | 169 (36.3%) | 189 | 18 |
| WA reddit | 0 (0.0%) | 0 | 0 |
| WA shopping | 0 (0.0%) | 0 | 0 |
| WA shopping_admin | 0 (0.0%) | 0 | 0 |

WA carries no reference images at all. This is the structural reason WA is a genuine out-of-distribution test for representation routing rather than a third VWA site: the visual-grounding demand that motivates SoM is simply absent.


## 3. Evaluation machinery

| corpus | page_image_query | program_html | string_match | url_match |
|---|---|---|---|---|
| VWA classifieds | 2 | 31 | 78 | 131 |
| VWA reddit | 28 | 85 | 70 | 51 |
| VWA shopping | 12 | 293 | 132 | 42 |
| WA reddit | 0 | 95 | 11 | 58 |
| WA shopping | 0 | 62 | 88 | 57 |
| WA shopping_admin | 0 | 91 | 88 | 28 |

Combinations actually used (a task may carry more than one checker):

- **VWA classifieds** — `url_match` ×125, `string_match` ×72, `program_html` ×29, `string_match + url_match` ×6, `page_image_query + program_html` ×2
- **VWA reddit** — `program_html` ×70, `string_match` ×64, `url_match` ×41, `page_image_query` ×13, `page_image_query + program_html` ×12, `string_match + url_match` ×6
- **VWA shopping** — `program_html` ×280, `string_match` ×129, `url_match` ×38, `page_image_query` ×6, `page_image_query + program_html` ×6, `program_html + url_match` ×4
- **WA reddit** — `program_html + url_match` ×58, `program_html` ×37, `string_match` ×11
- **WA shopping** — `string_match` ×88, `program_html` ×47, `url_match` ×42, `program_html + url_match` ×15
- **WA shopping_admin** — `string_match` ×88, `program_html` ×66, `program_html + url_match` ×25, `url_match` ×3


## 4. Difficulty annotations (VWA only)

Human labels shipped by the benchmark authors, independent of any model's behaviour — so crossing them against measured SR is an external check, not a circular one. WA ships none; those rows are omitted rather than defaulted.


**reasoning difficulty**

| corpus | easy | hard | hrad | medium |
|---|---|---|---|---|
| VWA classifieds | 68 (29.1%) | 90 (38.5%) | 0 (0.0%) | 76 (32.5%) |
| VWA reddit | 58 (27.6%) | 73 (34.8%) | 0 (0.0%) | 79 (37.6%) |
| VWA shopping | 172 (36.9%) | 131 (28.1%) | 1 (0.2%) | 162 (34.8%) |

**visual difficulty**

| corpus | easy | hard | medium | mediun |
|---|---|---|---|---|
| VWA classifieds | 69 (29.5%) | 86 (36.8%) | 78 (33.3%) | 1 (0.4%) |
| VWA reddit | 95 (45.2%) | 41 (19.5%) | 74 (35.2%) | 0 (0.0%) |
| VWA shopping | 168 (36.1%) | 123 (26.4%) | 175 (37.6%) | 0 (0.0%) |

**overall difficulty**

| corpus | easy | hard | medium |
|---|---|---|---|
| VWA classifieds | 42 (17.9%) | 104 (44.4%) | 88 (37.6%) |
| VWA reddit | 55 (26.2%) | 76 (36.2%) | 79 (37.6%) |
| VWA shopping | 108 (23.2%) | 147 (31.5%) | 211 (45.3%) |


## 5. Instruction length

| corpus | words (p10 / med / p90 / max) | chars (med / max) |
|---|---|---|
| VWA classifieds | 12 / 18.0 / 28 / 56 | 100.0 / 300 |
| VWA reddit | 14 / 21.0 / 35 / 74 | 107.0 / 413 |
| VWA shopping | 11 / 17.0 / 27 / 54 | 85.5 / 273 |
| WA reddit | 9 / 16.0 / 25 / 28 | 91.0 / 186 |
| WA shopping | 8 / 13.0 / 24 / 46 | 77.5 / 256 |
| WA shopping_admin | 8 / 12.0 / 19 / 24 | 64.0 / 139 |

Intent length matters for cost accounting: it is the one prompt component identical across all six observation modes, so it sets a floor that no representation choice can reduce.

