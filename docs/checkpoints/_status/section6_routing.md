---
type: paper-section
status: deferred-pending-data
progress: 40
blocker: Phase 1a router data land (post-Pass-1 baseline + Pass-2 router fire)
eta: pending Phase 1a fire
owner: 我
section_num: 6
---

# Section 6 Routing — ⏳ Phase 1a fire pending (no §6 prose draft)

⏳ **AUROC ≥ baseline diagnostic (commit `9d7e99f`)** is **Phase 1a fire pending** — current `aggregate_routing_auroc.py` output is finite-cell descriptive only, not a deployable-signal claim until Phase 1a 6 cells land. Status was previously `✅` which over-claimed pre-data signal as a paper §6 finding; downgraded per /stress A2.6b P1-11-B (codex Mode B) 2026-05-18 (B-1287 — `✅` was unqualified status absent §6 prose + scope guard, would mislead Bases-view reviewers).

Hard blocker: §6 prose written **after** router data land — per /stress A2.6b user Q3=A decision 2026-05-18 (defer §6 skeleton until router cells fire, prevents over-commit to prose direction during data flux).

When §6 prose lands, scope guard at top of §6 mandated per codex P1-1-B*/P1-5-B*/P1-8-B*:
> "All router and AUROC claims in this section are finite-design claims over the 6 planned VWA classifieds/reddit × B0/B1/B2 cells per prereg §2 H1 decision 3A FE estimand; LOCO cross-cell + RE analyses are Appendix-D sensitivity diagnostics, NOT primary estimand. Cross-population (out-of-distribution) generalization committed as future work via Phase 1b shop fire post-workshop submission per prereg §7."

**Paper 真正最值钱工作量** (substrate); prose-write deferred to post-router-fire to avoid codex-round churn.
