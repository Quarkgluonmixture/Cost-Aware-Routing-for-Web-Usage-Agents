\subsection*{8.1 Scope \& external-validity bounds}

This study is deliberately narrow: three sites (classifieds, reddit, shopping; Phase 1a workshop scope = classifieds + reddit only with shopping deferred to Phase 1b main paper), one benchmark family (VisualWebArena plus WA-mini-style task structure), and three baselines: **B0** Qwen3-VL-235B-A22B via proxy API, **B1** Qwen3-VL-4B local, and **B2** Gemma3-VL `google/gemma-3-4b-it` local (added 2026-05-14 as cross-family robustness check at 4B parameter parity vs B1 (a parameter-parity comparison, not a strict matched-capability anchor — Gemma vs Qwen lineage have different vision encoders, pretraining, and instruction-tuning, so this is a robustness-direction test, not a controlled-capability ceiling)). The blast radius is external validity, not the internal comparison: our claim is that a phantom routing space exists for Qwen+Gemma agents on VWA-style tasks under controlled 3-baseline comparison (B0/B1 within-family across capability scale; B1/B2 cross-family at the same 4B parameter scale (parameter-parity, not matched-capability)), not that every VLM, browser benchmark, or production site will expose the same arm ordering \citep{zhou2024webarena,koh2024visualwebarena,deng2023mind2web,drouin2024workarena}. Mid-layer mechanism evidence (activation patching, layer probes, logit lens, SAE) is deferred to a follow-up paper per advisor scope-flip 2026-05-14; the present paper rests on the behavioural two-knob account (text representation × prompt family) plus the cost-aware router results. Shopping has weaker intermediate coverage than classifieds/reddit, so site-level generalization is reported cell-by-cell rather than averaged into a universal web-agent claim. This affects Sections 1, 5, and 6 by bounding language about universality and reproducibility: B1 + B2 behavior is byte-reproducible from released artifacts on a self-hosted A100 VWA Docker stack (HF SHAs pinned per `pre_run/locked_versions.md`), while B0 is verifiable from traces and replayable subject to API access; cross-architecture claims beyond Qwen+Gemma, including GPT-4o-family and Llama-family claims, remain outside the paper.

\subsection*{8.2 Construct validity \& evaluator threats}

VWA success labels are imperfect measurements of task completion, especially around `ua_match` GPT-judge drift, the `string_match` `fuzzy_threshold` misnomer, brittle `program_html` selectors, and `finish_wrong_state` episodes. The blast radius is measurement-side: these four evaluator-class threats can flip individual labels or inflate raw success, but they do not redefine the task universe or the mode definitions; full appendix prose is kept in Section 4.X.1--4.X.4 rather than duplicated here. Following the 2026-05-14 §139.8 FP-architecture restructure, the dominant evaluator FP source (the LLM judge scoring an empty prediction as correct in `llm_fuzzy_match` / `llm_ua_match`) is fixed at the VWA evaluator itself (B-91 source-level patch in submodule branch `p79-patches`), and N/A tasks are excluded at task-load (`exclude_na_tasks: true`, 73 N/A tasks across VWA+WA), so raw `success` from the fixed evaluator is the **internal-P79 paper-grade outcome** (`adjusted_success ≡ success`); the prior post-hoc `compute_adjusted_success` filter and 3-variant sensitivity ladder (`na_fp` / `eval_fp` / `visual_fp`) are retired and retained only for pre-§139.8 archive contamination disclosure in Appendix D \citep{lipton2018troubling,neurips2024checklist}. We deliberately do not call this outcome ``canonical'' in a cross-paper sense: the LLM judge is configurable via the `VWA_EVAL_MODEL` environment variable (default `gpt-4o-mini`, differing from upstream VWA's deprecated `gpt-4-1106-preview` --- see Section 3.5 ``LLM-judge model disclosure''), so SR comparisons against the original VWA, WebArena-Verified, or PAE baselines require a judge-model-equivalent rerun before they are paired. This affects Sections 3 and 4: raw `success` is the headline metric for internal P79 paired comparisons (DOM ↔ P-SoM, baseline ↔ phantom within the same cell), the source-level fixes are disclosed transparently, and claims are defensible as representation effects rather than judge artifacts under the (fixed) evaluator at the chosen judge model; cross-paper SR comparisons are scoped to qualitative direction rather than point-estimate equivalence.

\subsection*{8.3 Internal-validity threats: known scaffold bugs}

Several scaffold bugs were real: the `in_viewport_ratio` operator-precedence bug exposed clipped DOM text, early scroll actions suffered direction-convention confusion, and Stage 2B/2C mechanism inputs came from pre-Phase-A archived browser states. The blast radius is bounded because these failures are mode-uniform within the relevant comparisons: the viewport bug affects DOM-derived text and Phantom-SoM's `[SOM_MARKS]` source together; scroll-direction confusion is a trajectory-execution threat rather than an evaluator rule; and Stage 2 uses frozen prompt/screenshot inputs, so Phase-A dispatch bugs affect which step an agent reached, not the model's forward pass on that saved step. This affects cross-mode interpretation in Sections 3--5: we treat environment failures as measurement threats and disclose their blast radius rather than folding them into cognitive claims.

\paragraph{Action-vocabulary cross-baseline asymmetry (B-406 / /stress A1.22 P2-20-C Gemini, 2026-05-17).} Coordinate-format emission rate differs systematically across baselines: empirical archive shows B0 emits pixel-format coordinates 15.6\% of clicks vs B1 35.3\% (a 2.3\(\times\) gap, source \texttt{section3\_definition.md:137}); B2's rate is to-be-determined post-Phase-1a fire. The wrapper accepts both normalized and pixel formats interchangeably via value-based heuristic (\texttt{vwa\_wrapper.py:360-377}), so the execution-layer behaviour is identical, but the model-internal coordinate-format preference is part of each baseline's capability bundle (B0's tool schema permits both; B1/B2 prompted JSON tends to emit pixels more often). Cross-baseline SR claims that depend on click precision in dense UI components should be read with this disclosure in mind --- coordinate format preference is not pure representation effect but a measurement-vocabulary confound we deliberately do not standardize away, because standardizing post-hoc would obscure the deployment-relevant behaviour (B0 ships through the proxy as-deployed with its tool-schema preferences).

\subsection*{8.4 Pre-vs-post-hoc analyses \& retracted framings}

Pre-vs-post-hoc discipline applies to behavioural claims; the mid-layer mechanism story is paper-2 scope per advisor 2026-05-14 and is not asserted here. The blast radius is epistemic status: preregistered H1--H3 gate the deployment and structural claims, H4 is exploratory, and H5--H6 are post-hoc explanatory layers on behavioural data. The negative-results registry records 12 retracted framings, including task-0 over-interpretation, the reverse-null claim overturned by the N=15 reverse sample, and the rejected selection-bias explanation. The single registry-backed empirical framing that survives audit for paper-1 is the **four-fold drop-in property** (cost ≈ DOM, lower image-stage latency, usable routing signal, positive drop-one oracle); the sparse L11/L17 activation-patching mechanism is retained in the paper-2 working draft (`paper_drafts/section5_mechanism.md` is the paper-2 stub, not part of paper-1). This affects Sections 1 and the deferred mechanism paper-2: the present paper can claim confirmed, registry-backed evidence for the four-fold drop-in property, but not a preregistered exact-layer prediction or a universal single-task circuit.

\subsection*{8.5 Statistical \& methodological limits}

The statistical design is adequate for medium effects but underpowered for small per-cell effects and exact-layer micro-effects. The blast radius is precision, not directionality: Holm-Bonferroni is applied across the six canonical tested layers (L0/5/11/17/23/29), not all 36 cached layers; this matches the disclosed post-hoc grid but should not be read as a full-layer search correction \citep{holm1979sequentially,wang2023interpretability,zhang2024patching}. Bootstrap intervals use task-paired resampling, random-effects meta-analysis is limited to cells with N>=10 to avoid unstable tau-squared estimates, and complete-case deletion handles crashes or missing artifacts without imputation. Exclusions are listwise only, at <=5% per cell under the B6 lock, so multiple imputation would add modeling assumptions without materially changing paired denominators. Power analysis shows N=15 mechanism cells are not powered for small mid-layer effects (roughly Cohen's d below 0.65 at alpha=0.05), while site-level SR cells mainly detect 4--7pp effects. This affects Sections 4--5: null or marginal cells are interpreted as low-power evidence, and pooled estimates are paired with per-cell uncertainty.

\subsection*{8.6 Mechanism evidence — deferred to follow-up paper}

The mid-layer activation-patching evidence (L11/L17 effects, task-conditional sparseness, content-specific vs generic-injection control) lives in the paper-2 working draft (\texttt{paper\_drafts/section5\_mechanism.md}). It is not asserted in this paper. The paper-1 mechanism account is the behavioural two-knob view introduced in Section~5: text representation shapes exploration, while prompt wording tunes commitment confidence. Mid-layer mechanism analysis is deferred to a follow-up paper per advisor scope decision 2026-05-14 (B-124 phantom-limb purge 2026-05-15 per gemini Mode C P1-5). The blast radius is therefore claim-boundary clarity, not a missing limitation: paper-1 does not depend on activation-patching results.

\subsection*{8.7 Compute, cost, and sustainability bounds}

Compute and carbon accounting are approximate because runs span DGX Spark, UCL Condense A100, and UCL Myriad V100/A100 profiles. The blast radius is absolute energy reporting: the per-cell table reports GPU-hours, proxy-API USD cost, and kg-CO2 estimates with hardware provenance, but cross-machine power variation limits precision \citep{qiu2025modserve}. Relative comparisons inside a cell remain valid because modes share the same hardware, benchmark site, and evaluator path. This affects the cost part of the four-fold drop-in claim: token/API cost and latency comparisons are primary, while carbon totals are disclosed as bounded estimates rather than a fine-grained lifecycle assessment. **Carbon emission provenance**: \texttt{co2e\_kg = total\_energy\_kwh \(\times\) 0.220} where \(0.220\) kg/kWh = 220 g/kWh is the UK national grid 2024 average carbon intensity (set via \texttt{carbon\_intensity\_g\_per\_kwh: 220} in \texttt{configs/exp\_v2\_base.yaml}, explicit override of the \texttt{REGION\_INTENSITY\_G\_PER\_KWH["uk"] = 257} default in \texttt{p79/experiment/energy\_tracker.py:49}; the 257 figure is IEA-2023 and is decorative because the explicit per-config 220 wins at the priority chain at \texttt{energy\_tracker.py:238-243}). Per-step \texttt{total\_energy\_kwh} is real measurement via pynvml on the A100 Condenser host (\texttt{NVIDIAPowerReader} + \texttt{LightweightEnergyTracker} priority chain in \texttt{p79/experiment/energy\_tracker.py:351-389}), aggregated over the per-step wall-clock duration. **PUE = 1.0 assumption (dock-power only)**: facility-level overhead such as cooling, networking, and rack-side conversion losses is not included; effective grid intensity would scale by the host data-centre's PUE (typically 1.3-1.5 for university HPC \citep{strubell2019energy}), making the published kg-CO2 numbers a lower bound rather than a calibrated value.

<!-- Bibkey audit 2026-05-12: lipton2018troubling, neurips2024checklist, holm1979sequentially, wang2023interpretability, zhang2024patching all verified present in paper.bib. -->

