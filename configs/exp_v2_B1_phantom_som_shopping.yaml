# B1 Phantom-SoM (§103) — VWA shopping
# B1 4B local on VWA shopping (待 Myriad 独占 GPU 跑 final)

defaults:
  - configs/exp_v2_base.yaml

experiment:
  phase: "phase1"
  name: "B1_phantom_shopping"

task:
  include_sites: ["shopping"]

runtime:
  max_steps: 30
  resume: true

variables:
  primary:
    observation_mode: ["phantom_som"]

backends:
  default_backend: "local_4b"
  local_4b:
    max_new_tokens: 4096

