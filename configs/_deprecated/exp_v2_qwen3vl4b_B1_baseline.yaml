defaults:
  - configs/exp_v2_base.yaml

experiment:
  phase: "phase1"
  name: "qwen3vl4b_B1_baseline_phase1"

task:
  include_sites: ["shopping"]

runtime:
  max_steps: 30
  resume: true

variables:
  primary:
    observation_mode: ["dom", "som", "vision"]

backends:
  default_backend: "local_4b"
  local_4b:
    max_new_tokens: 384
    min_free_vram_gb: 0

baselines:
  run_b0: false
