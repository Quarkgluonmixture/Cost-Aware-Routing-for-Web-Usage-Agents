# B2 P-text ablation — WA shopping_admin (Magento admin panel)
# DOM prompt + SoM marks 文本 + 无图;与 B2 phantom_som wa_shopping_admin 唯一差: system prompt

defaults:
  - configs/exp_v2_wa_base.yaml

experiment:
  phase: "phase1"
  name: "B2_phantom_text_wa_shopping_admin"

task:
  include_sites: ["shopping_admin"]
  site_configs:
    shopping_admin: "external/visualwebarena/config_files/wa/test_shopping_admin.raw.json"

runtime:
  max_steps: 30
  resume: true

variables:
  primary:
    observation_mode: ["phantom_dom"]

backends:
  default_backend: "local_gemma"
  local_gemma:
    max_new_tokens: 4096
    min_free_vram_gb: 0

baselines:
  run_b0: false
